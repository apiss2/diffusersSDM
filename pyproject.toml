[project]
name = "diffuserssdm"
version = "0.1.0"
description = "Semantic Image Synthesis via Diffusion Models implemented with diffusers"
authors = [{name = "AutoGPT"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "diffusers>=0.34.0",
    "torch==2.5.1",
    "torchvision==0.20.1",
    "transformers>=4.55.0",
    "numpy",
    "Pillow",
    "pyyaml",
    "tqdm",
]

[project.urls]
homepage = "https://example.com"
