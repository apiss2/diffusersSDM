[project]
name = "diffuserssdm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "albumentations>=2.0.8",
    "diffusers>=0.34.0",
    "torch==2.5.1",
    "torchvision==0.20.1",
    "transformers>=4.55.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
